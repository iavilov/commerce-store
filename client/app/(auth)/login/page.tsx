import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Login1',
  description: 'Login page',
};

const Page = () => {
  return (
    <p>Login page</p>
  );
};

export default Page;