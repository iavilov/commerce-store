import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: {
    absolute: 'Device'
  }
};


const Page = () => {
  return (
    <p>Device page</p>
  );
};

export default Page;